$("<%= escape_javascript(render partial: '/novels/comment-card', locals: { comment: @comment }) %>").appendTo("#comment-cards");

$('form').find("textarea, :text, select").val("").end().find(":checked").prop("checked", false);

setTimeout(function(){
  alert("コメントしました！");
},150);
